import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const store = new Vuex.Store({
  // 全局数据
  state: {
  },
  // 全局方法
  mutations: {
  },
  // 全局计算属性
  getters: {
  },
  // 全局异步
  actions: {
  },
});
export default store;
